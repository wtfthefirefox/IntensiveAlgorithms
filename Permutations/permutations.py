from typing import List


def permutations(n: int) -> List[List[int]]:
    """
    Returns all permutations of numbers from 1 to n (including)

    Args:
        n (int): number

    Returns:
        List[int]: permutations of numbers
    """

    return []
