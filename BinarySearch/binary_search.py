from typing import List


def binary_search(value: int, array: List[int]) -> int:
    """
    Returns index of value in ascending sorted array

    Args:
        value (int): value to find position of
        array (List[int]): list of sorted numbers

    Returns:
        int: index of value
    """

    return 0
